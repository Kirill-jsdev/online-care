import { Send } from "lucide-react";

const TelegramIcon = () => {
  return <Send className="w-5 h-5 text-white group-hover:text-pink-400 transition-colors" strokeWidth={2.2} />;
};

export default TelegramIcon;
