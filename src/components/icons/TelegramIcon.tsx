import { Send } from "lucide-react";

const TelegramIcon = () => {
  return <Send className="w-5 h-5 text-cyan-400 group-hover:text-cyan-300 transition-colors" strokeWidth={2.2} />;
};

export default TelegramIcon;
